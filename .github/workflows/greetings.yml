name: Memer Greetings

on: [pull_request]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
      - name: Create comment
        uses: jungwinter/comment@v1
        id: create
        with:
          type: create
          body: >
           Hello @${{ github.actor }}, thanks 🤗 for opening this PR! Pls wait while the maintainer(s) review it
           Meanwhile have a look at this meme :)
           > ![meme](${{meme}})
